image: ruby:2.3

stages:
- test

test:
  stage: test
  before_script:
  - bundle install --deployment
  - .circleci/install.sh
  - mkdir .circleci/tmp
  - bundle exec rake start
  script:
  - bundle exec rake test
  - bundle exec rake lint
